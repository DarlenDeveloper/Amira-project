{
  "name": "amira-explore-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Amira Luxury — Explore web shop (React), live on the shared amira-interiors Firebase backend.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.4",
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^6.4.3",
    "wrangler": "^4.106.0"
  }
}
